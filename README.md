# Recipe Graph Engine

Neo4j 그래프 데이터베이스 기반 레시피 추천 엔진 API

## 개요

사용자가 보유한 재료를 입력하면 만들 수 있는 레시피를 추천하고, 건강 상태/다이어트 목표/식단 유형에 맞는 맞춤형 추천을 제공합니다.

### 주요 기능

- **재료 기반 추천**: 보유 재료로 만들 수 있는 레시피 추천
- **다이어트 추천**: 칼로리 제한 및 목표(벌크업/다이어트) 기반 추천
- **건강 맞춤 추천**: 건강 상태(당뇨/고혈압 등) 고려한 안전 레시피 추천
- **비건/채식 추천**: 식단 유형별 호환 레시피 추천
- **페르소나 모드**: 12가지 페르소나별 맞춤 응답 및 특화 추천
- **유사 레시피**: 재료 공유 기반 유사 레시피 탐색
- **재료 페어링**: 함께 자주 사용되는 재료 추천
- **부족 재료 확인**: 레시피 완성에 필요한 추가 재료 안내

### 페르소나 모드 (12종)

**FREE 티어**
| 모드 | 아이콘 | 상태 | 설명 |
|-----|:----:|:----:|------|
| 엄마밥 | 👩‍🍳 | ✅ | 기본 집밥 레시피 추천 |
| 자취생 | ⚡ | ✅ | 20분 이내 초간단 레시피 |
| 집밥요리사 | 🏠 | 🔜 | 편안한 가정식 레시피 |
| 알뜰살림 | 💰 | 🔜 | 가성비 저예산 레시피 |

**PREMIUM 티어**
| 모드 | 아이콘 | 상태 | 설명 |
|-----|:----:|:----:|------|
| 다이어트 | 💪 | ✅ | 칼로리/벌크업 목표 기반 추천 |
| 건강맞춤 | 🏥 | ✅ | 당뇨/고혈압 등 건강 상태별 추천 |
| 비건 | 🌈 | ✅ | 비건/채식 호환 레시피 |
| 흑백요리사 | 🖤 | ✅ | 고급/손님 접대용 레시피 |
| 벌크업코치 | 🏋️ | 🔜 | 고단백 근육 증가 레시피 |
| 아이밥상 | 👶 | 🔜 | 아이/유아 영양 균형 레시피 |
| 손님초대 | 🎉 | 🔜 | 파티/접대용 대용량 레시피 |
| 한식장인 | 🏛️ | 🔜 | 정통 한식 전통 조리법 |

> ✅ 현재 사용 가능 | 🔜 Coming Soon

### 데이터 현황

| 항목 | 수량 |
|-----|------|
| 레시피 | 2,240개 |
| 재료 | 9,160개 |
| 레시피-재료 관계 | 23,309개 |
| 유사 레시피 관계 | 13,083개 |
| 재료 페어링 관계 | 1,768개 |

## 설치

### 요구사항

- Python 3.11+
- Neo4j 5.x
- Docker (선택)

### 환경 설정

```bash
# 저장소 클론
git clone <repository-url>
cd recipe

# 가상환경 생성 및 활성화
python -m venv .venv
source .venv/bin/activate

# 의존성 설치
pip install -r requirements.txt

# 환경변수 설정
cp .env.example .env
# .env 파일 수정 (Neo4j 연결 정보 등)
```

### Neo4j 실행 (Docker)

```bash
docker compose up -d
```

### 데이터 로드

```bash
# 공공데이터 크롤링
python scripts/crawl_public_data.py

# 만개의레시피 크롤링
python scripts/crawl_10000recipe.py

# Neo4j에 데이터 로드
python scripts/recipe_loader.py
```

### API 서버 실행

```bash
uvicorn src.api.app:app --host 0.0.0.0 --port 8000
```

## API 문서

### Base URL

```
http://localhost:8000
```

### Swagger UI

```
http://localhost:8000/docs
```

---

## 엔드포인트

### 1. 상태 확인

```http
GET /
```

**응답**
```json
{
  "status": "ok",
  "message": "Recipe Graph Engine API"
}
```

---

### 2. 데이터베이스 통계

```http
GET /stats
```

**응답**
```json
{
  "stats": {
    "recipes": 2240,
    "ingredients": 9160,
    "relations": 23309
  }
}
```

---

### 3. 재료 기반 추천 (FREE)

```http
POST /recommend
```

**요청**
```json
{
  "ingredients": ["돼지고기", "김치", "두부", "대파"],
  "persona": "엄마밥",
  "user_name": "회원",
  "min_coverage": 60,
  "limit": 5
}
```

| 필드 | 타입 | 필수 | 설명 |
|-----|------|:----:|------|
| ingredients | string[] | O | 보유 재료 목록 |
| persona | string | X | 응답 페르소나 (기본: 엄마밥) |
| user_name | string | X | 사용자 이름 (기본: 회원) |
| min_coverage | int | X | 최소 재료 매칭률 % (기본: 60) |
| limit | int | X | 결과 수 (기본: 5) |

**응답**
```json
{
  "recipes": [
    {
      "name": "김치찌개",
      "category": "찌개",
      "cuisine": "한식",
      "time_minutes": 30,
      "difficulty": "쉬움",
      "coverage": 80.0,
      "missing_count": 1,
      "calories": 520,
      "protein": 38
    }
  ],
  "message": "우리 회원이 뭐 해먹을까~\n\n**이거 해먹어! 김치찌개**\n..."
}
```

---

### 4. 다이어트 추천 (PREMIUM)

```http
POST /recommend/diet
```

**요청 - 칼로리 제한**
```json
{
  "ingredients": ["닭가슴살", "양파", "브로콜리"],
  "max_calories": 400,
  "limit": 5
}
```

**요청 - 목표 기반**
```json
{
  "ingredients": ["닭가슴살", "계란"],
  "goal": "벌크업",
  "limit": 5
}
```

| 필드 | 타입 | 필수 | 설명 |
|-----|------|:----:|------|
| ingredients | string[] | O | 보유 재료 목록 |
| max_calories | int | X | 최대 칼로리 (기본: 500) |
| goal | string | X | 목표: 다이어트, 벌크업, 유지, 저탄수, 고단백 |
| limit | int | X | 결과 수 (기본: 5) |

**응답**
```json
{
  "recipes": [
    {
      "name": "닭가슴살 샐러드",
      "category": "무침",
      "calories": 250,
      "protein": 35,
      "carbs": 10,
      "fat": 8,
      "coverage": 66.0
    }
  ],
  "message": "회원님! 오늘도 건강한 선택 하러 오셨네요 💪\n..."
}
```

---

### 5. 건강 맞춤 추천 (PREMIUM)

```http
POST /recommend/health
```

**요청**
```json
{
  "ingredients": ["두부", "시금치", "버섯"],
  "condition": "당뇨",
  "limit": 5
}
```

| 필드 | 타입 | 필수 | 설명 |
|-----|------|:----:|------|
| ingredients | string[] | O | 보유 재료 목록 |
| condition | string | O | 건강 상태: 당뇨, 고혈압, 통풍, 신장질환, 고지혈증 |
| limit | int | X | 결과 수 (기본: 5) |

**응답**
```json
{
  "recipes": [
    {
      "name": "두부 스테이크",
      "category": "구이",
      "coverage": 50.0,
      "calories": 280,
      "safe": true
    }
  ],
  "message": "회원님의 건강 상태를 고려한 맞춤 식단입니다.\n..."
}
```

---

### 6. 비건/채식 추천 (PREMIUM)

```http
POST /recommend/vegan
```

**요청**
```json
{
  "ingredients": ["두부", "양파", "고추장"],
  "diet_type": "비건",
  "limit": 5
}
```

| 필드 | 타입 | 필수 | 설명 |
|-----|------|:----:|------|
| ingredients | string[] | O | 보유 재료 목록 |
| diet_type | string | X | 식단 유형: 비건, 채식, 페스코, 락토, 오보 (기본: 비건) |
| limit | int | X | 결과 수 (기본: 5) |

**응답**
```json
{
  "recipes": [
    {
      "name": "두부 비빔밥",
      "category": "밥",
      "protein": 15,
      "coverage": 40.0
    }
  ],
  "message": "회원님, 오늘도 지구와 함께하는 식사! 🌍\n..."
}
```

---

### 7. 유사 레시피

```http
GET /recipes/{recipe_name}/similar?limit=5
```

**경로 파라미터**
| 파라미터 | 설명 |
|---------|------|
| recipe_name | 레시피 이름 |

**쿼리 파라미터**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|:----:|------|
| limit | int | X | 결과 수 (기본: 5) |

**응답**
```json
{
  "recipe": "김치찌개",
  "similar": [
    {
      "name": "부대찌개",
      "category": "찌개",
      "difficulty": "쉬움",
      "time_minutes": 30,
      "similarity": 0.4,
      "shared_ingredients": 4
    }
  ]
}
```

---

### 8. 부족 재료 확인

```http
GET /recipes/{recipe_name}/missing?my_ingredients=돼지고기&my_ingredients=김치
```

**경로 파라미터**
| 파라미터 | 설명 |
|---------|------|
| recipe_name | 레시피 이름 |

**쿼리 파라미터**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|:----:|------|
| my_ingredients | string[] | O | 보유 재료 목록 |

**응답**
```json
{
  "recipe": "김치찌개",
  "missing": [
    {
      "ingredient": "두부",
      "amount": 150,
      "unit": "g",
      "category": "기타",
      "optional": false
    }
  ]
}
```

---

### 9. 재료 페어링

```http
GET /ingredients/{ingredient_name}/pairings?limit=10
```

**경로 파라미터**
| 파라미터 | 설명 |
|---------|------|
| ingredient_name | 재료 이름 |

**쿼리 파라미터**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|:----:|------|
| limit | int | X | 결과 수 (기본: 10) |

**응답**
```json
{
  "ingredient": "간장",
  "pairings": [
    {
      "ingredient": "설탕",
      "category": "조미료",
      "count": 130
    }
  ]
}
```

---

### 10. 재료 자동완성

```http
GET /ingredients/autocomplete?prefix=고추&limit=10
```

**쿼리 파라미터**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|:----:|------|
| prefix | string | O | 검색어 (최소 1자) |
| limit | int | X | 결과 수 (기본: 10) |

**응답**
```json
{
  "ingredients": [
    {
      "name": "고추",
      "category": "채소"
    },
    {
      "name": "고추장",
      "category": "조미료"
    }
  ]
}
```

---

### 11. 페르소나 모드 목록

```http
GET /modes
```

**응답**
```json
{
  "modes": [
    {
      "id": "UMMA",
      "name": "엄마밥",
      "icon": "👩‍🍳",
      "tier": "FREE",
      "description": "따뜻한 엄마의 마음으로 집밥 레시피 추천",
      "tone": "따뜻하고 친근한 어머니",
      "focus": ["실용성", "속도", "가성비"],
      "query_mode": "general"
    }
  ],
  "total": 12,
  "tiers": {
    "FREE": [...],
    "PREMIUM": [...]
  }
}
```

---

### 12. 페르소나 모드 상세

```http
GET /modes/{mode_name}
```

**경로 파라미터**
| 파라미터 | 설명 |
|---------|------|
| mode_name | 모드 이름 (예: 자취생밥상, 벌크업코치) |

**응답**
```json
{
  "id": "QUICK",
  "name": "자취생밥상",
  "icon": "⚡",
  "tier": "FREE",
  "description": "자취생을 위한 초간단 레시피 (20분 이내)",
  "tone": "현실적이고 효율적인 자취 선배",
  "formality": "반말 (캐주얼)",
  "focus": ["시간 절약", "최소 재료", "간편함"],
  "query_mode": "quick",
  "greeting_template": "{user_name}! 오늘도 빠르게 해결하자 ⚡",
  "encouragement": "5분이면 끝! 배고플 때 최고지 ㅋㅋ"
}
```

---

### 13. 통합 모드 기반 추천

```http
POST /recommend/mode
```

모든 페르소나 모드를 하나의 엔드포인트로 통합하여 사용할 수 있습니다.

**요청 - 자취생 (20분 간편식)**
```json
{
  "ingredients": ["계란", "김치", "밥"],
  "mode": "자취생",
  "user_name": "자취생",
  "limit": 5
}
```

**요청 - 다이어트 (칼로리 제한)**
```json
{
  "ingredients": ["닭가슴살", "브로콜리"],
  "mode": "다이어트",
  "max_calories": 400,
  "limit": 5
}
```

**요청 - 다이어트 (벌크업 목표)**
```json
{
  "ingredients": ["닭가슴살", "계란"],
  "mode": "다이어트",
  "goal": "벌크업",
  "limit": 5
}
```

**요청 - 건강맞춤**
```json
{
  "ingredients": ["두부", "시금치"],
  "mode": "건강맞춤",
  "condition": "당뇨",
  "limit": 5
}
```

**요청 - 비건**
```json
{
  "ingredients": ["두부", "버섯"],
  "mode": "비건",
  "diet_type": "비건",
  "limit": 5
}
```

| 필드 | 타입 | 필수 | 설명 |
|-----|------|:----:|------|
| ingredients | string[] | O | 보유 재료 목록 |
| mode | string | O | 페르소나 모드명 |
| user_name | string | X | 사용자 이름 (기본: 회원) |
| limit | int | X | 결과 수 (기본: 5) |
| max_calories | int | X | 최대 칼로리 (다이어트코치용) |
| goal | string | X | 목표 (다이어트코치용): 다이어트, 벌크업, 유지, 저탄수, 고단백 |
| condition | string | X | 건강 상태 (건강맞춤용): 당뇨, 고혈압, 통풍, 신장질환, 고지혈증 |
| diet_type | string | X | 식단 유형 (무지개요리사용): 비건, 채식, 페스코, 락토, 오보 |
| max_minutes | int | X | 최대 조리 시간 (자취생밥상용, 기본: 20) |

**응답**
```json
{
  "recipes": [
    {
      "name": "계란볶음밥",
      "category": "밥",
      "cuisine": "한식",
      "time_minutes": 15,
      "difficulty": "쉬움",
      "coverage": 67.0,
      "missing_count": 1,
      "calories": 480,
      "protein": 18,
      "carbs": 62,
      "fat": 16
    }
  ],
  "message": "자취생! 오늘도 빠르게 해결하자 ⚡\n\n**초간단! 계란볶음밥**\n⏱️ 15분 완성!\n재료 매칭: 67% | 부족: 1개\n\n5분이면 끝! 배고플 때 최고지 ㅋㅋ"
}
```

**모드별 특화 쿼리**

| 모드 | 쿼리 특성 |
|-----|----------|
| 엄마밥 | 재료 매칭률 기반 일반 추천 |
| 자취생 | 20분 이내 + 쉬움/보통 난이도 필터 |
| 다이어트 | 칼로리 제한 또는 벌크업 목표 기반 |
| 건강맞춤 | 건강 상태별 위험 레시피 제외 |
| 비건 | 비건/채식 호환 레시피 |
| 흑백요리사 | 재료 매칭률 기반 고급 추천 |

---

## 프로젝트 구조

```
recipe/
├── config/
│   └── settings.py          # 환경 설정
├── data/
│   ├── raw/                  # 원본 데이터
│   └── processed/            # 처리된 데이터
├── scripts/
│   ├── crawl_public_data.py  # 공공데이터 크롤러
│   ├── crawl_10000recipe.py  # 만개의레시피 크롤러
│   ├── extract_kaggle_korean.py  # Kaggle 데이터 추출
│   ├── process_manual_files.py   # 수동 파일 처리
│   ├── recipe_loader.py      # Neo4j 데이터 로더
│   └── structurizer.py       # LLM 기반 구조화
├── src/
│   ├── api/
│   │   └── app.py           # FastAPI 앱
│   ├── engines/
│   │   ├── query_engine.py  # Cypher 쿼리 엔진
│   │   └── persona_engine.py # 페르소나 응답 생성
│   ├── models/
│   │   └── recipe.py        # 데이터 모델
│   └── utils/
│       ├── neo4j_client.py  # Neo4j 클라이언트
│       └── llm_client.py    # LLM 클라이언트
├── .env.example
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## 그래프 스키마

### 노드

| 노드 | 속성 |
|-----|------|
| Recipe | name, category, cuisine, time_minutes, difficulty, servings, total_calories, total_protein, total_carbs, total_fat, tags, spicy_level, description, steps, tips, suitable_for, avoid_for |
| Ingredient | name, category, vegan |
| Goal | name (다이어트, 벌크업, 유지, 저탄수, 고단백) |
| Condition | name (당뇨, 고혈압, 통풍, 신장질환, 고지혈증) |
| Diet | name (비건, 채식, 페스코, 락토, 오보) |

### 관계

| 관계 | 설명 | 속성 |
|-----|------|------|
| (Ingredient)-[:REQUIRED_FOR]->(Recipe) | 재료가 레시피에 필요함 | amount, unit, optional, prep |
| (Recipe)-[:SUITABLE_FOR]->(Goal) | 레시피가 목표에 적합함 | score |
| (Recipe)-[:AVOID_FOR]->(Condition) | 건강 상태에서 피해야 함 | severity |
| (Recipe)-[:COMPATIBLE_WITH]->(Diet) | 식단과 호환됨 | - |
| (Recipe)-[:SIMILAR_TO]->(Recipe) | 유사한 레시피 | score, shared_ingredients |
| (Ingredient)-[:PAIRS_WELL]->(Ingredient) | 함께 자주 사용됨 | count |

## 데이터 출처

- 식품의약품안전처 조리식품 레시피 DB (공공데이터포털)
- 만개의레시피 (https://www.10000recipe.com)
- Kaggle What's Cooking 데이터셋
- 전북특별자치도 음식만드는법
- 농림수산식품교육문화정보원 고수요리법

## 라이선스

MIT License
