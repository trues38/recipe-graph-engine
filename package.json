{
  "name": "recipe-graph-engine-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "postinstall": "cd web && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
